cmake_minimum_required (VERSION 2.8)
project (client)
include_directories (./)
aux_source_directory(. DIR_SRCS)
find_library( zmq-lib zmq )
#find_package(zmq)
set(CMAKE_C_FLAGS       "${CMAKE_C_FLAGS} -lzmq")
add_executable(client.out ${DIR_SRCS})
target_link_libraries (client.out zmq)
